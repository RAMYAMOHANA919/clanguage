#include<stdio.h>
void main()
{
    int count=0,i,num;
    while(1)
    {
        printf("enter a number ");
        scanf("%d",&num);
        if(num==0)
        break;
        count+=num;
    }
    printf("The sum is %d",count);
    
}
